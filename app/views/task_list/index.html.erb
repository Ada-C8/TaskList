<!-- TaskList#index -->

<!-- <h1 class="index-heading">Task List</h1> -->
<div class="all-content-container">

  <div class="tasks-container">

    <h2 class="your-tasks">Your Tasks:</h2>
    <ul class="list-container">
      <% @tasks.each do |task| %>
        <li class="each-task">

              <%if task.status%>
                <div class="strike-through">
                  <%=task.id%>. <%=task.name%>
                </div>
                <div class="completion-date"> --> completed on: <%= task.completion_date%></div>

              <%else%>
                <div class="no-strike-through">
                  <%=task.id%>. <%=task.name%> --> not yet complete
                </div>
              <%end%>
        </li>
        <li class="task-actions">
          <%=link_to (task.status ? "unmark complete" : "mark complete"), mark_complete_path(task.id), method: :patch, alt: "mark complete", class: "mark-complete" %> | <%=link_to "details", task_path(task.id)%> | <%=link_to "edit", edit_task_path(task.id)%> |  <%=link_to "delete", destroy_task_path(task.id), method: :delete %>
        </li>
      <%end%>
    </ul>

  </div>

</div>
<!-- <p>Find me in app/views/task_list/index.html.erb</p> -->
