<h2><%= @task.name %> </h2>
<ul>
  <% if @task.status == "complete" %>
    <% print_status = "mark incomplete" %>
    <% else %>
    <% print_status = "mark complete" %>
  <% end %>
  <li><%= link_to print_status, mark_task_path(@task.id), method: :patch %></li>
  <li><%= link_to "edit", edit_task_path(@task.id) %></li>
  <li><%= link_to "delete", delete_task_path(@task.id), method: :delete, data: { confirm: "Really?" } %></li>
</ul>
<h4><%= @task.completion_date %>
  <% if Date.parse(@task.completion_date) > Date.today() %>
    <%='😸' %>
  <% else %>
    <%='🙀' %>
  <% end %>
<%= @task.status %>
  <% if @task.status == "complete" %>
    <%= '😻' %>
  <% else %>
    <%= '😿' %>
  <% end %>
</h4>
<p class="description">
<%= @task.description %>
</p>
