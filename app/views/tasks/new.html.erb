<h1>Tasks#new</h1>

<%= form_for @task do |t| %>
  <%= t.label :name %>
  <%= t.text_field :name %>

  <%= t.label :description %>
  <%= t.text_field :description %>

  <%= t.label :completion_date %>
  <%= t.text_field :completion_date %>

  <%= t.submit "add task" %>
<% end %>
