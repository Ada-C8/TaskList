<h1>Tasklist</h1>

<ol>
<% @tasks.each do |task| %>
  <li>
    <strong>
      <%= link_to(task[:task_name], task_path(task[:id]), alt: 'link to', class:"task_list" ) %>
    </strong>
  </li>
<% end %>
</ol>
