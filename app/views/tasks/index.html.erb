<h1>Maria's Task List</h1>

<section id="lists">

<% @tasks.each do |task| %>
  <li>
   <%= link_to(task.name, task_path(task.id)) %>
 </li>
   <% end %>
 </section>

  <%= link_to "Add New Task", new_task_path %>
