<h1>Task List</h1>
<ul>
  <% @tasks.each do |task|  %>
    <li>
      <%= task[:name] %> (<%= task[:status]%>), started on <%= task[:created]%> and due by <%= task[:due] %>
    </li>
  <% end %>
</ul>
