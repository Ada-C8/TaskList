<h2>YOUR TASKS</h2>
<ol>
  <% @tasks.each do |task|%>
    <li>
      <strong><%= link_to "#{task.title}", task_path(task.id) %></strong>
      <p>
        <em><%= task.description%></em>
      </p>
      <p>
        <%= link_to "#{task.complete}", complete_task_path(task.id)  %>  | <%= link_to "Edit", edit_task_path(task.id) %> | <%= task.due_date %>
      </p>
    </li>
  <% end %>
</ol>
