<h1>Tasks#index</h1>
<p>Find me in app/views/tasks/index.html.erb</p>

<p>
  <%= "These are the breaks" %>
</p>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <%= "#{task[:name]} is #{task[:status]}" %>
  </li>
  <% end %>
</ul>
