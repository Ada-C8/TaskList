<body>
  <div class="to_do_list">
    <h2>To Do:</h2>
    <ul>
      <% @tasks.each do |task| %>
      <li>
        <%= link_to(task.name, task_path(task.id)) %>
      </li>
      <%end%>
    </ul>
  </div>
</body>
