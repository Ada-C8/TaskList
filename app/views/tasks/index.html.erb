<h3>Tasks</h3>
  <!-- <p>Tasks coming soon..</p>  -->

  <%= @tasks.each do |task| %>
      <div class="task">
        <strong><%= task.title %></strong>
        <%= task.description %>
      </div>
  <% end %>
