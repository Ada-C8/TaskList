<h2>edit</h2>

<%= form_for @task do |f| %>
   <%= f.label :task %>
   <%= f.text_field :task %>

   <%= f.label :description %>
   <%= f.text_field :description %>

   <%= f.label :due %>
   <%= f.text_field :due, placeholder: "e.g., 2017-9-21" %>

   <%= f.submit "Save changes" %>
 <% end %>
