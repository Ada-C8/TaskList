<%= form_for @task do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.label :description %>
  <%= f.text_area :description %>

  <%= f.date_select :due_date %>

  <%= f.submit "Save" %>
<% end %>

<%= button_to "Cancel", tasks_path, method: :get %>
